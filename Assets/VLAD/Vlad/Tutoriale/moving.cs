using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class moving : MonoBehaviour
{
    //public KeyCode Вперед;
   // public KeyCode Назад;
   // public KeyCode Вправо;
   // public KeyCode Влево;
    //public Transform camera;
    // Start is called before the first frame update
  /*  void Start()
    {
         Cursor.visible = false;
        // Cursor.lockState = CursorLockMode.None;
    }

    // Update is called once per frame
    void Update()
    {
        if(Input.GetKey(Вперед)){
            Vector3 d = camera.position;
            Vector3 forward = d + new Vector3(0, 0, 1f);
           // Vector3 forward = Камера.position + new Vector3(0, ViewAngle / 2f, 0);
      // EnemyEye.position
            camera.position = forward;  
        }
    }*/
}
